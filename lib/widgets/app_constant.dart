import 'package:flutter/material.dart';

class AppConstants {
  static const double mobilePadding = 16.0;
  static const double webPadding = 24.0;
  static const double mobileFontSize = 14.0;
  static const double webFontSize = 16.0;
  static const double mobileIconSize = 48.0;
  static const double webIconSize = 64.0;
  static const double mobileButtonPaddingH = 12.0;
  static const double webButtonPaddingH = 16.0;
  static const double mobileButtonPaddingV = 8.0;
  static const double webButtonPaddingV = 10.0;

  static const List<String> aylar = [
    'Ocak',
    'Şubat',
    'Mart',
    'Nisan',
    'Mayıs',
    'Haziran',
    'Temmuz',
    'Ağustos',
    'Eylül',
    'Ekim',
    'Kasım',
    'Aralık',
  ];

  // Renkler
  static const Color primaryColor = Color(0xFF4B2E83); // Mor
  static const Color accentColor = Color(0xFFF45B69); // Mercan
  static const Color backgroundColor = Color(0xFFFCF7F2); // Krem
  static const Color textColor = Color(0xFF2D2A4A); // Lacivert yazı

  // Padding
  static const double cardPadding = 12.0;
  static const double gridSpacing = 16.0;

  // Diğer sabitler
  static const double iconSizeMobile = 40.0;
  static const double iconSizeWeb = 60.0;

  static const double textSizeMobile = 13.0;
  static const double textSizeWeb = 16.0;

  static const List<Color> cardColors = [
    Color(0xFFEDE7F6), // Açık Mor
    Color(0xFFE3F2FD), // Açık Mavi
    Color(0xFFE8F5E9), // Açık Yeşil
    Color(0xFFFFF3E0), // Açık Turuncu
    Color(0xFFFFEBEE), // Açık Pembe
  ];
}
